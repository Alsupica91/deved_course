use cdnjs.com for... something I did not quite get it
	use for Font Awesome, whatever that is
<i> is for Icon, this is what the above is for

<select> with <option> to make a drop down menu
<div class="select">
        <select name="todos" class="filter-todo">
          <option value="all">All</option>
          <option value="completed">Completed</option>
          <option value="uncompleted">Uncompleted</option>
        </select>
</div>